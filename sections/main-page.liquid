<div class="container prose sm:prose-lg lg:prose-xl text-center">
  <h1>{{ page.title }}</h1>

  <p>{{ page.content }}</p>
</div>

{% schema %}
{
  "name": "t:sections.page.name",
  "tag": "section"
}
{% endschema %}